<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginLeft="10dp"
    android:layout_marginRight="10dp"
    android:layout_marginBottom="10dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".PreviousTexts"
    android:orientation="vertical">


    <TableLayout
        android:id="@+id/rankingsTable"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:textAlignment="center"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/btBack"
        >

        <TableRow
            android:id="@+id/tablerow"
            android:divider="#c4c4">
            <TextView
                android:id="@+id/rankingsName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Player || "
                android:textSize="26dp"
                android:textAlignment="center"
                android:layout_gravity="center|center_horizontal" />

            <TextView
                android:id="@+id/rankingsScore"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Score || "
                android:textAlignment="center"
                android:textSize="26dp"
                android:layout_gravity="center|center_horizontal" />

            <TextView
                android:id="@+id/rankingsGamemode"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text=" Mode"
                android:textSize="26dp"
                android:textAlignment="center"
                android:layout_gravity="center|center_horizontal"
                />
        </TableRow>
    </TableLayout>

    <Button
        android:id="@+id/btBack"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Voltar"
        android:onClick="backToMain"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/rankingsTable"/>

</android.support.constraint.ConstraintLayout>
